#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main(){
  
  ifstream ifs("iris.data");
  if(!ofs){
    cout << "failed opening iris.data" << endl;
    exit(1);
  }

  
  return 0;
}

